# ─── Supabase Auth (Project Settings → API) ───────────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=https://gloynjzlbqsowomryqwl.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# ─── Prisma / Database (Project Settings → Database → Connection string) ──────
#
# HOW TO FIND THESE:
#   Supabase Dashboard → Project Settings → Database → "Connection string" tab
#   Switch the dropdown to "Transaction pooler" for DATABASE_URL
#   Switch the dropdown to "Session pooler"     for DIRECT_URL
#
# ❌ WRONG (direct host — firewalled by Supabase, causes "Can't reach database" error):
#   DATABASE_URL="postgresql://postgres:pass@db.gloynjzlbqsowomryqwl.supabase.co:5432/postgres"
#
# ✅ CORRECT — Transaction pooler (port 6543) for DATABASE_URL:
DATABASE_URL="postgresql://postgres.gloynjzlbqsowomryqwl:YOUR_DB_PASSWORD@aws-0-ap-south-1.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1"

# ✅ CORRECT — Session pooler (port 5432) for DIRECT_URL (used only for migrations):
DIRECT_URL="postgresql://postgres.gloynjzlbqsowomryqwl:YOUR_DB_PASSWORD@aws-0-ap-south-1.pooler.supabase.com:5432/postgres"

# NOTE: Replace YOUR_DB_PASSWORD with the password you set when creating the Supabase project.
# NOTE: The region in the host (ap-south-1) may differ — copy the exact URL from Supabase dashboard.
